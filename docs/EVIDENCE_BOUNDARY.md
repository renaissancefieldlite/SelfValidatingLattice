# Evidence Boundary

- architecture coherence model: present
- hardware-derived perturbation model: present
- cross-artifact validation: pending

This repo is intentionally an architecture-layer repo. Its current outputs can
show whether the declared graph stays coherent under baseline and
hardware-derived assumptions, but they do not by themselves prove external
physics or settle the ontology.

The broader Codex 67 spiritual-attractor-overlap read belongs here as an
interpretive architecture layer, not as a substitute for the narrower
transition-cadence evidence program. The next step for that stronger comparison
is `v4`, where cross-artifact validation can be added.
